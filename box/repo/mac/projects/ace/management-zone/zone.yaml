config:
    - management-zone-staging: "management-zone.json"
    - management-zone-prod: "management-zone.json"

management-zone-staging:
    - environment: "staging"
    - name: "mac-mz-staging"

management-zone-prod:
    - environment: "production"
    - name: "mac-mz-production"

